import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        mode: "dark",
        background: {
            default: "#202124",
        },
    },
    typography: {
        fontSize: 14,
    },
});

export default theme;
